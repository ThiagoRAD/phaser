const config = {
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  backgroundColor: "#020202"
};
const game = new Phaser.Game(config);

function preload() {}

function create() {}

function update() {}
